# Claude API
ANTHROPIC_API_KEY=sk-ant-...

# Arc Testnet wallet (EVM)
# Seller: the address that receives USDC payments
SELLER_ADDRESS=0x...
# Buyer: private key of the agent that pays (NEVER commit the real key)
BUYER_PRIVATE_KEY=0x...

# App
PORT=8000
API_BASE_URL=http://localhost:8000
