# Claude API
ANTHROPIC_API_KEY=sk-ant-...

# Arc Testnet wallet (EVM)
# Seller: the address that receives USDC payments
SELLER_ADDRESS=0x...
# Buyer: private key of the agent that pays (NEVER commit the real key)
BUYER_PRIVATE_KEY=0x...

# 0G Storage (optional — falls back to BUYER_PRIVATE_KEY if not set)
# Fund with testnet tokens at https://faucet.0g.ai
ZG_PRIVATE_KEY=0x...

# App
PORT=8000
API_BASE_URL=http://localhost:8000
